/*
Algorithms
1.Prompt the user for numbers
2.Stop prompting user inputs -1
    do while loop for the prompt to happen at least once
    do/while(userInputs != -1)
    do{
    }while(userInputs != -1){
    }
3.Math functions
 */

//1.Prompt the user for number
var userInputs = prompt("Enter Positive Numbers");

//2.Stop prompting user inputs -1
do{

}while(userInputs != -1){
    //Mean Calculation
    var mean;
    var total = 0;
    for(var i = 0; i< userInputs.length; i++){
        total += userInputs[i]
    }
    mean = total / userInputs.length;

    //Median Calculation


    //Maximum Calculation

    //Minimum Calculation

}